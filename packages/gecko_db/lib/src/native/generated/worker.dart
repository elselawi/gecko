// This file is automatically generated, so please do not edit it.
// @generated by `flutter_rust_bridge`@ 2.12.0.

// ignore_for_file: invalid_use_of_internal_member, unused_import, unnecessary_import

import 'frb_generated.dart';
import 'package:flutter_rust_bridge/flutter_rust_bridge_for_generated.dart';

/// one group of child rows sharing the same foreign-key value (parent
/// id). The worker classifies matching child rows by FK so Dart receives
/// pre-grouped candidates instead of re-decoding each row's FK field.
class GroupedChildEntries {
  /// The encoded foreign-key value (the parent id bytes).
  final Uint8List parentId;

  /// Child rows in row-key order whose FK equals [parent_id].
  final List<(Uint8List, Uint8List)> entries;

  const GroupedChildEntries({required this.parentId, required this.entries});

  @override
  int get hashCode => parentId.hashCode ^ entries.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is GroupedChildEntries &&
          runtimeType == other.runtimeType &&
          parentId == other.parentId &&
          entries == other.entries;
}

/// Storage-level size/health report
class StorageStats {
  /// Bytes the database file occupies on disk (physical).
  final BigInt physicalBytes;

  /// Sum of key + value bytes across every table (logical payload).
  final BigInt logicalBytes;

  /// Number of tables (user + reserved metadata).
  final BigInt tableCount;

  /// Open point-in-time MVCC snapshots.
  final BigInt openSnapshots;

  /// Committed write batches so far.
  final BigInt commitSequence;

  const StorageStats({
    required this.physicalBytes,
    required this.logicalBytes,
    required this.tableCount,
    required this.openSnapshots,
    required this.commitSequence,
  });

  @override
  int get hashCode =>
      physicalBytes.hashCode ^
      logicalBytes.hashCode ^
      tableCount.hashCode ^
      openSnapshots.hashCode ^
      commitSequence.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is StorageStats &&
          runtimeType == other.runtimeType &&
          physicalBytes == other.physicalBytes &&
          logicalBytes == other.logicalBytes &&
          tableCount == other.tableCount &&
          openSnapshots == other.openSnapshots &&
          commitSequence == other.commitSequence;
}

/// One mark-synchronizing / mark-synced / mark-failed transition: the encoded
/// collection + recordId of a selected sync-state record, its
/// `localMutationId` (the change-log LSN), and the fully rewritten change
/// record to persist in both sync state and the matching change-log records.
class SyncTransitionUpdate {
  /// DefaultWireCodec-encoded collection string.
  final Uint8List collection;

  /// DefaultWireCodec-encoded recordId string.
  final Uint8List recordId;

  /// The change-log LSN (the record's localMutationId) whose records are
  /// rewritten.
  final BigInt localMutationId;

  /// The fully rewritten change record (encoded map) to persist.
  final Uint8List newState;

  const SyncTransitionUpdate({
    required this.collection,
    required this.recordId,
    required this.localMutationId,
    required this.newState,
  });

  @override
  int get hashCode =>
      collection.hashCode ^
      recordId.hashCode ^
      localMutationId.hashCode ^
      newState.hashCode;

  @override
  bool operator ==(Object other) =>
      identical(this, other) ||
      other is SyncTransitionUpdate &&
          runtimeType == other.runtimeType &&
          collection == other.collection &&
          recordId == other.recordId &&
          localMutationId == other.localMutationId &&
          newState == other.newState;
}
